[FilterPlugin]
; Add non-standard files here when packaging the plugin.

; ScriptCore ships no scripts of its own -- it is the host. Each plugin or GameFeature that
; does ships its own Content/Script/main.js and lists it in its own FilterPlugin.ini.
